<%= form_with(model: follower, local: true,url: ride_followers_path )do |form| %>
  <% if follower.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(follower.errors.count, "error") %> prohibited this follower from being saved:</h2>

      <ul>
      <% follower.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="inner-holder">
  <label>Your Pick Up Location</label>
   <%= form.text_field :pick_up_location, placeholder: 'Pick Up Location', autofocus: true, autocomplete: "pick_up_location", required: true  %>
  <br><br><p>Enter your pick up location else leave blank and submit</p>
  </div>
  <div class="actions">
    <%= form.submit "Subscribe", class: "submit" %>
  </div>
<% end %>
